# MantisNXT Environment Variables (Example)
#
# Copy to `.env.local` and replace values:
#   cp env.example .env.local
#
# NOTE: `.env.local` is gitignored. Never commit real secrets.

# --- Required ---
# UNIFIED DATABASE: All modules (main app, SPP, SOH) use this single database.
# Neon pooled connection string (recommended). Use `sslmode=require`.
# Example format:
# postgresql://<user>:<password>@<host>-pooler.<region>.neon.tech/<db>?sslmode=require
DATABASE_URL=postgresql://neondb_owner:REPLACE_ME@ep-example-pooler.gwc.azure.neon.tech/neondb?sslmode=require&pgbouncer=true

# App secrets (must be >= 32 chars)
JWT_SECRET=dev_jwt_secret_change_me_at_least_32_chars
SESSION_SECRET=dev_session_secret_change_me_at_least_32_chars

# --- Optional (Database Fallback) ---
# UNIFIED DATABASE: All modules (main app, SPP, SOH) use the same DATABASE_URL above.
# Optional fallback connection string (uses same unified database):
# ENTERPRISE_DATABASE_URL=postgresql://.../neondb?sslmode=require&pgbouncer=true
#
# DEPRECATED (backwards compatibility only - do not use):
# NEON_SPP_DATABASE_URL and SUPPLIER_DATABASE_URL are no longer needed.

# --- POS Module ---
# Separate database for the POS system (NXT_POS)
# Copy the DATABASE_URL from your NXT_POS project's .env
POS_DATABASE_URL=postgresql://neondb_owner:REPLACE_ME@ep-pos-example.neon.tech/pos_db?sslmode=require

# --- Marketplace Module ---
# Separate database for the Marketplace (NXT_Marketplace)
# Copy the DATABASE_URL from your NXT_Marketplace project's .env
MARKETPLACE_DATABASE_URL=postgresql://neondb_owner:REPLACE_ME@ep-marketplace-example.neon.tech/marketplace_db?sslmode=require

# --- Optional ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_MARKETING_APP_URL=http://localhost:3000/marketing-app
NEXT_PUBLIC_MARKETING_WHATSAPP_NUMBER=5511932162209
NEXT_PUBLIC_MARKETING_STORE_NAME=Luizinho
NEXT_PUBLIC_MARKETING_STORE_TAGLINE=Electronics Store
REDIS_URL=redis://localhost:6379

# --- Social Media & Marketing Module ---
# Separate database for Social Media module (optional - uses main DATABASE_URL if not set)
SOCIAL_MEDIA_DATABASE_URL=postgresql://neondb_owner:REPLACE_ME@ep-social-example.neon.tech/social_media_db?sslmode=require

# Token encryption for social media credentials (32 bytes, base64)
SOCIAL_MEDIA_ENCRYPTION_KEY="AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA="

# Meta (Facebook/Instagram) OAuth
META_APP_ID=""
META_APP_SECRET=""
META_REDIRECT_URI="https://your-domain.com/api/social-media/oauth/meta/callback"
META_WEBHOOK_VERIFY_TOKEN=""

# WhatsApp Business API (Meta Cloud API)
WHATSAPP_BUSINESS_PHONE_NUMBER=""
WHATSAPP_BUSINESS_ACCOUNT_ID=""
WHATSAPP_ACCESS_TOKEN=""
WHATSAPP_CLOUD_API_PHONE_NUMBER_ID=""
WHATSAPP_CLOUD_API_ACCESS_TOKEN=""

# Twilio WhatsApp API (Recommended Alternative)
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_WHATSAPP_FROM="whatsapp:+14155238886"

# TikTok OAuth
TIKTOK_CLIENT_KEY=""
TIKTOK_CLIENT_SECRET=""
TIKTOK_REDIRECT_URI="https://your-domain.com/api/social-media/oauth/tiktok/callback"

# Meta Marketing & Commerce (Optional - for advanced features)
META_PIXEL_ID=""
META_BUSINESS_ID=""
META_AD_ACCOUNT_ID=""

# Vercel Blob Storage (for media uploads)
BLOB_READ_WRITE_TOKEN=""




