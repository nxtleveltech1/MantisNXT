# MantisNXT Environment Variables (Example)
#
# Copy to `.env.local` and replace values:
#   cp env.example .env.local
#
# NOTE: `.env.local` is gitignored. Never commit real secrets.

# --- Required ---
# Neon pooled connection string (recommended). Use `sslmode=require`.
# Example format:
# postgresql://<user>:<password>@<host>/<db>?sslmode=require&pgbouncer=true
DATABASE_URL=postgresql://neondb_owner:REPLACE_ME@ep-example-pooler.gwc.azure.neon.tech/neondb?sslmode=require&pgbouncer=true

# App secrets (must be >= 32 chars)
JWT_SECRET=dev_jwt_secret_change_me_at_least_32_chars
SESSION_SECRET=dev_session_secret_change_me_at_least_32_chars

# --- Optional (Multi-DB) ---
# If you run the 2-tier architecture, set these to the correct Neon DBs.
# ENTERPRISE_DATABASE_URL=postgresql://.../mantis_issoh?sslmode=require&pgbouncer=true
# NEON_SPP_DATABASE_URL=postgresql://.../mantis_spp?sslmode=require&pgbouncer=true

# --- Optional ---
NEXT_PUBLIC_APP_URL=http://localhost:3000
REDIS_URL=redis://localhost:6379



