# Fix for @ai-sdk/anthropic zod v4 peer dependency conflict
# @ai-sdk/anthropic@1.2.12 requires zod@^3.0.0 but we use zod@4.1.9
# This allows the build to proceed while the AI SDK team updates compatibility
; legacy-peer-deps removed after aligning zod to ^3.22.4 for AI SDK compatibility

# Vercel deployment configuration
engine-strict=false
