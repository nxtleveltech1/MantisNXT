# MantisNXT Environment Configuration Example
# Copy this file to .env.local and update with your values

# Database Configuration (Neon PostgreSQL)
NEON_DATABASE_URL="postgresql://user:password@host/database"
NEON_SPP_DATABASE_URL="postgresql://user:password@host/spp_database"

# API Authentication (Optional - for development)
# Uncomment to bypass auth on specific GET endpoints
# ALLOW_PUBLIC_GET_ENDPOINTS=/api/core/selections,/api/core/selections/active,/api/core/selections/catalog

# JWT Secret (for production auth)
# JWT_SECRET="your-secret-key-here"

# API Configuration
# API_KEY="your-api-key" # Optional: Set if using X-API-Key authentication

# Next.js Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Feature Flags
# ENABLE_AUTH=false # Set to false to disable authentication entirely (DEV ONLY)
